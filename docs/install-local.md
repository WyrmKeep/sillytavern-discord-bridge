# Local Install

1. Run `npm ci`.
2. Run `npm run build`.
3. Enable `enableServerPlugins: true` in SillyTavern.
4. Copy the built server plugin into `SillyTavern/plugins/discord-bridge`.
5. Copy the built UI extension into the configured third-party extensions folder.
