# Architecture

The bridge is a paired SillyTavern server plugin and UI extension. SillyTavern remains the source of truth for Claude reverse-proxy settings; the bridge owns Discord session mapping and bridge-owned chat persistence.
